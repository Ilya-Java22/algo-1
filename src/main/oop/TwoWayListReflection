Суть у меня та же.

Единственное, не стал явно прописывать конструкторы в TwoWayList и LinkedList, так как они дефолтные,
никакой смысловой нагрузки не несут, а в Java дефолтные конструкторы можно не указывать, плюс при их вызове будет
вызван конструктор родителя (ParentList), а там он определен и реализован (как и основное хранилище, которое преобразует
команда clear(), содержащаяся в этом конструкторе).

Также я написал реализацию части членов абстрактного класса ParentList, так как фразу из Задания 3 "...и дополните реализацией"
я понял именно в таком ключе.

Ну и у меня есть запросы статусов запросов is_tail() и is_head(), так как еще в прошлом занятии я посчитал необходимым
определить для них предусловие ("список не пустой"), см. соответствующее задание и рефлексию. Пока не понимаю, почему мы можем
поступить иначе.